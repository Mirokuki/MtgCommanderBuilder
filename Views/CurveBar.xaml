<UserControl x:Class="MtgCommanderBuilder.Views.CurveBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="140" d:DesignWidth="40">

    <Grid VerticalAlignment="Bottom">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Number Label -->
            <RowDefinition Height="*"/>    <!-- Graph Bar -->
        </Grid.RowDefinitions>

        <!-- Count Text Label at top -->
        <TextBlock Name="TxtCount" Grid.Row="0" Text="0" FontSize="11" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,4"/>

        <!-- The Bar itself -->
        <Border Name="BarVisual" Grid.Row="1" Width="18" CornerRadius="4,4,0,0" VerticalAlignment="Bottom" Height="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#e6caa13e" Offset="0.0"/>
                    <GradientStop Color="#33caa13e" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#caa13e" BlurRadius="6" ShadowDepth="0" Opacity="0.3"/>
            </Border.Effect>
        </Border>
    </Grid>
</UserControl>
